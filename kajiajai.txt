
//nodo* cursor;
nodo* slow=h_list;
nodo* fast;
if(h_list==NULL){
    nodo *newnodo;
    newnodo=malloc(sizeof(nodo));
    newnodo->val=valore;
    h_list=newnodo;
    newnodo->next=NULL;
}
else {
    while(slow!=NULL){
        fast=slow->next;
        if(fast==NULL){
            nodo* newnodo;
            newnodo=malloc(sizeof(nodo));
            newnodo->val=valore;
            newnodo->next=NULL;
            slow->next=newnodo;
        }
        else if(slow->val<valore && fast->val>valore){
            nodo *newnodo;
            newnodo=malloc(sizeof(nodo));
            newnodo->val=valore;
            newnodo->next=fast;
            slow->next=newnodo;
        }
        slow=fast;
    }
}